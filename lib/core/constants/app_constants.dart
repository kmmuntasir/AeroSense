/// Shared dimension and layout constants for the app.
class AppConstants {
  AppConstants._();

  // Typography
  static const double tempFontSize = 72;
  static const double metricValueFontSize = 36;

  // Border radii
  static const double cardRadius = 16.0;
  static const double heroRadius = 20.0;
}
